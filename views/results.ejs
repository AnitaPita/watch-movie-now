<!DOCTYPE html>
<html>
<head>
    <title><%= title%></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
</head>
<body>

<% include  templates/header.ejs%>
<h3>Your search for "<%=search %>" in <%=typesearch %> returned <%=imdb.length %> result(s).</h3>

<ul>
    <% for(var i=0; i<imdb.length;i++) { %>
    <form method="post" action="/details">
        <input type="image" name="thisname" src="<%= "https://image.tmdb.org/t/p/w342"+imdb[i].poster_path %>">
        <input type = "hidden" name="moviename" value="<%=imdb[i].movietitle%>" >
    </form>
    <% }%>

    <% if(imdb.length==0) { %>
    <h2>No movies were found containing "<%=search %>".</h2>
    <% }%>
</ul>

</body>
</html>